# 测试用模板

⚠️ **保真度测试必须用真实业务文件。**

自己造的简化模板里本来就没什么可丢的,会骗过你 —— 本库作者就是这么错过一次的:
先用一个 4KB 的空模板测,一切正常,直到拿真实模板才发现 23 个零件只剩 10 个。

请把带以下元素的真实模板(**脱敏后**)放进本目录:

- 表单控件(复选框、下拉框)
- 图形对象、图片
- 打印设置(打印区域、页眉页脚)
- 数据验证、条件格式
- 合并单元格

命名为 `real_template.xlsx`,`test_fidelity.py` 会自动发现并跑保真度断言;
文件不存在时该测试会 skip 而不是失败。
